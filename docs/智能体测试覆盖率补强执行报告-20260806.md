# 智能体测试覆盖率补强执行报告-20260806

> 执行日期：2026-08-06；第二阶段更新：2026-08-07
> 测试范围：`src/apps/agent`，并包含必要的证券解析边界回归
> 结论：✅ 本轮补强完成；Agent 独立覆盖率硬门禁、全量测试、契约、构建和本地运行态均通过

## 1. 执行摘要

本轮不是单纯追逐行覆盖率，而是围绕用户已遇到的 Tool 绑定路径缺失和 Agent 内部错误，补齐 Workflow、Model/Tool 审计、引用、终态事务、并发、权限、SSE、Provider 与压力场景。第二阶段继续补齐 Provider 配置、研究报告和多资产 Tool 边界。最终 Agent 全量执行 76 个测试套件、668 个测试，全部通过。

| 指标       | 补强前 | 第一阶段 | 第二阶段 | 总提升（百分点） | 独立硬门禁 | 结果 |
| ---------- | -----: | -------: | -------: | ---------------: | ---------: | ---- |
| Statements | 66.38% |   83.75% |   86.30% |           +19.92 |      85.00% | PASS |
| Branches   | 50.34% |   67.37% |   71.10% |           +20.76 |      70.00% | PASS |
| Functions  | 72.35% |   88.57% |   91.20% |           +18.85 |      90.00% | PASS |
| Lines      | 69.45% |   88.21% |   90.56% |           +21.11 |      90.00% | PASS |

第二阶段四项目标全部达到，门禁已同步提高到 `85% / 70% / 90% / 90%`，会阻止覆盖率退回目标以下。

## 2. 环境

| 项目       | 版本/方式 |
| ---------- | --------- |
| Node.js    | v24.5.0 |
| pnpm       | 10.18.3 |
| Docker     | 29.6.2 |
| PostgreSQL | 17.10（Docker Compose） |
| Redis      | 7.4.10（Docker Compose） |
| 模型       | 自动化使用 scripted/fake Provider，不产生真实模型费用 |

## 3. 核心业务回归

| 场景组 | 结果 | 关键验证 |
| ------ | ---- | -------- |
| Workflow/Tool 绑定 | PASS | 空候选、可选/必需 Tool、依赖跳过、Schema 非法、旧证券简称回归 |
| Model 审计与预算 | PASS | Provider 成功后预算超限仍保留 SUCCEEDED 审计；审计失败不伪造模型失败 |
| 引用完整性 | PASS | 引用修复、二次校验、舍入/千分位数字、无效引用受控失败 |
| 事务与竞态 | PASS | 幂等请求、CAS、重复消费、取消/完成竞态、终态不可逆、事件顺序 |
| 权限与脱敏 | PASS | 租户隔离、严格 DTO、Tool/Provider 错误与敏感字段脱敏 |
| HTTP/SSE E2E | PASS | 创建、发送、状态、重生成、断线恢复、历史版本不可变、失败卡片 |
| Provider 原生协议 | PASS | OpenAI Responses、Anthropic Messages、流式事件与 usage 归一化 |
| PostgreSQL/Redis 集成 | PASS | 10 个集成套件、77 个断言实际执行，无默认跳过 |

四个直接故障相关文件的覆盖结果：

| 文件 | Statements | Branches | Functions | Lines |
| ---- | ---------: | -------: | --------: | ----: |
| `tool-result-binding.ts` | 98.66% | 96.00% | 100% | 100% |
| `workflow-model.service.ts` | 94.81% | 84.46% | 100% | 96.66% |
| `validate-citations.node.ts` | 100% | 100% | 100% | 100% |
| `workflow-tool.service.ts` | 96.15% | 88.57% | 100% | 98.86% |

第二阶段重点文件覆盖结果：

| 文件 | Statements | Branches | Functions | Lines |
| ---- | ---------: | -------: | --------: | ----: |
| `research-report.service.ts` | 95.30% | 85.71% | 97.67% | 97.86% |
| `market-multi-asset-tools.ts` | 100% | 85.10% | 100% | 100% |
| `model-provider-config.service.ts` | 88.93% | 83.33% | 90.24% | 93.81% |

## 4. 缺陷与修复

| Bug ID | 严重度 | 根因 | 修复与回归 |
| ------ | ------ | ---- | ---------- |
| AGT-20260806-001 | S1/P0 | 旧证券简称及空候选使必需 Tool 读取不存在路径 | 解析/绑定改为受控处理，并加入“因赛股份”→`300781.SZ` 回归 |
| AGT-20260806-002 | S1/P0 | 模型成功后本地预算异常被重复终态审计覆盖 | 恢复成功调用时重新执行 usage 限制，保持 ModelCall SUCCEEDED |
| AGT-20260806-003 | S2/P0 | fake Provider 仍触发数据库能力热加载 | fake/fixture 环境跳过 DB 热加载，测试启动与数据库配置解耦 |
| AGT-20260806-004 | S2/P1 | 引用数字解析不识别千分位 | 引用覆盖支持 `1,500` 等格式，并加入独立回归 |
| AGT-20260807-005 | S1/P0 | 已发布快照未复用 Provider 业务校验 | 活动快照进入路由前统一校验模型 ID、能力、成本、URL、上下文和重试预算 |

## 5. 性能、负载与压力结果

| 类型 | 规模 | 观测结果 |
| ---- | ---- | -------- |
| Workflow 基准 | 100 次 fake Workflow | p50 21.489ms，p95 22.423ms，p99 22.636ms |
| HTTP E2E | REST 创建/发送/状态 | create p95 4.91ms，send p95 24.00ms，status p95 3.11ms，错误率 0 |
| SSE 回放 | 断线后 replay | TTFB p95 77.61ms，完成 p95 77.71ms，错误率 0 |
| 阶梯负载 | 并发 1/5/10 | 吞吐约 1.98/7.76/9.38 req/s，错误率 0 |
| 并发幂等 | 同一会话 50 次并发 | CREATED 1、SKIPPED 49，无重复业务 Run |
| 批量会话 | 20 个会话 | 错误 0 |
| 压力边界 | 大上下文、Tool 预算、SSE 连接数 | 分别返回稳定错误码 6049/6019；连接上限返回 429，释放后可重连；无 OOM |

性能数字是本机首轮基线，不作为跨机器绝对 SLA；后续建议以相同环境 p95 退化不超过 20%、错误率不升高作为回归判断。

## 6. 门禁与运行验证

| 验证命令/对象 | 结果 |
| ------------- | ---- |
| `pnpm run test:agent:coverage` | PASS：76/76 suites，668/668 tests，83.378 秒；门禁 85/70/90/90 |
| `pnpm run agent:contracts:check` | PASS |
| `pnpm build` | PASS |
| Prisma Client 生成与迁移 | PASS；55 个迁移，无待执行项 |
| `app` 容器 | healthy；`GET /health` 返回 `status: ok` |
| `agent-worker` 容器 | 正常启动并完成 `agentWorker.bootstrap` |

独立门禁配置位于仓库根目录 `jest.agent.config.ts`，执行入口为 `package.json` 中的 `test:agent:coverage`。覆盖范围限定到 `src/apps/agent`，同时纳入 Agent 单元、集成和 E2E 测试。

## 7. 剩余风险与下一步

- Branches 当前 71.10%，已超过本轮 70% 门禁；若继续提高，下一轮建议以 75% 为目标，优先补 `observability`、`retrieval`、`conversation` 和剩余 Provider 流异常分支。
- 自动化 Provider 使用 fake/scripted 实现，能验证协议和状态机，但不能替代真实供应商限流、账单及区域网络抖动的小流量验收。
- 本轮 HTTP/SSE E2E 已通过；真实浏览器成功链路采用 2026-08-05 验收结果，没有伪造一次新的浏览器执行记录。

## 8. 最终结论

两阶段已连续完成。覆盖率从约 66%/50%/72%/69% 提升到 86.30%/71.10%/91.20%/90.56%，近期页面故障及非法 Provider 活动快照均有直接回归用例，数据库、Redis、SSE、并发、压力、契约、构建和本地运行态均通过。后续按提高后的独立门禁持续防回退即可。
