# 智能体有界上下文构建与工作流接入测试执行报告-20260721

> 范围：Batch 019 第四阶段 `ContextBuilder`、`load_context`、ModelCall manifest 与 stock research workflow
> 执行日期：2026-07-21
> 结论：✅ GO（本阶段后端范围）

## 1. 执行结论

有界上下文已接入 workflow。系统规则、Prompt/workflow、页面/会话状态、摘要、最近消息、Tool facts、有效用户记忆按冻结顺序构建；旧消息优先裁剪；系统规则和当前问题不可裁；仍超限返回 `AI_CONTEXT_TOO_LARGE`（6018）。

本阶段未新增公共 API。manifest 只保留 segment 类型、来源 ID、hash、token 和 warning；ModelCall 审计不保存完整消息正文。真实 PostgreSQL owner 隔离、摘要回退、确认记忆、删除/过期过滤和跨租户 canary 均通过。

## 2. 自动化结果

| 层级                                 | 结果                                                                                            |
| ------------------------------------ | ----------------------------------------------------------------------------------------------- |
| ContextBuilder 单元                  | 13/13 通过（顺序、预算、裁剪、Unicode、摘要 hash/gap/竞态、截止日、脱敏、200/100/50 benchmark） |
| workflow 单元                        | `stock-research.workflow.spec.ts` 通过；plan/synthesize 均使用新 segment 输入                   |
| ModelCall 单元                       | context window 预留、6018、manifest 审计脱敏通过                                                |
| 真实 PostgreSQL workflow integration | 2/2 通过：8 Step/2 ModelCall 完成；摘要+memory+跨租户 manifest                                  |
| Agent 相关回归                       | 28 suites；249 passed，63 skipped（skip 为既有外部/条件集成）                                   |
| Batch 018 fresh-DB HTTP/E2E          | 19/19 通过，含主链、Tool、故障、租户、SSE、PERF、LOAD、STRESS                                   |
| PERF/LOAD 子集                       | 3/3 通过；0 error、0 cross-flow、0 active Run/Outbox 残留                                       |
| TypeScript / build / lint            | `tsc --noEmit`、production build、lint baseline 全通过                                          |
| Prisma / formatting                  | `prisma validate/generate`、Prettier、`git diff --check` 全通过                                 |

## 3. 性能观测

环境：macOS arm64、Apple M2 Pro、10 logical CPU、Node v24.5.0、独立 PostgreSQL 16、fake model、独立 Redis；未操作主服务容器。

### 3.1 ContextBuilder benchmark

fixture：200 messages、100 active memories、50 Tool facts，重复 20 次；仅记录本机基线，不作为产品 SLO。

|     p50 |     p95 |      p99 |
| ------: | ------: | -------: |
| 2.16 ms | 2.56 ms | 14.99 ms |

### 3.2 Agent E2E PERF/LOAD

| 操作                             |       p50 |       p95 |       p99 |             吞吐 |
| -------------------------------- | --------: | --------: | --------: | ---------------: |
| REST create ×100                 |   2.38 ms |   2.70 ms |   3.27 ms |     415.81 req/s |
| REST send ×100                   |  17.45 ms |  20.09 ms |  33.29 ms |      54.78 req/s |
| REST status ×100                 |   2.64 ms |   2.94 ms |   3.13 ms |     374.93 req/s |
| SSE replay（100 events ×20）TTFB |  43.17 ms |  47.15 ms |  58.51 ms |   22.87 replay/s |
| SSE replay 完成时延              |  43.24 ms |  47.25 ms |  58.72 ms | 2282.71 events/s |
| Run load concurrency 1           | 404.59 ms | 404.59 ms | 404.59 ms |       2.47 run/s |
| Run load concurrency 5           | 390.93 ms | 391.13 ms | 391.13 ms |      12.78 run/s |
| Run load concurrency 10          | 591.23 ms | 637.37 ms | 637.37 ms |      15.69 run/s |

错误率均为 0。正式产品 p50/p95/p99、吞吐、成本 SLO 仍由 Batch 025/026 另行批准，不把本机基线写成产品阈值。

## 4. 缺陷与修复

| ID          | 严重度 | 发现                                                             | 修复                                                                              |
| ----------- | ------ | ---------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| CTX-BUG-001 | P1     | 新 segment 输入让旧 fake provider 读不到 task/facts，误触发 6017 | fake fixture 改为解析 recent/tool segments；生产协议不变                          |
| CTX-BUG-002 | P0     | 超大 conversation state 被摘要化后可能丢失 accepted cutoff       | 先从原始结构化 state 读取 cutoff，再裁显示 state                                  |
| CTX-BUG-003 | P0     | 摘要并发推进覆盖当前 trigger 时，recent 可能为空                 | 检测当前问题缺失，丢弃摘要并回退原始消息                                          |
| CTX-BUG-004 | P1     | 输入估算未预留目标模型输出窗口                                   | `WorkflowModelService` 按 Run 剩余和 `contextWindow - maxOutputTokens` 取较小预算 |

## 5. 运行隔离与清理

- PostgreSQL/Redis 使用独立临时容器和临时数据库；测试结束已删除。
- 主 `quant_app`、`quant_postgres`、`quant_redis` 未连接、未迁移、未写入。
- 原始消息、摘要历史和记忆历史未被 ContextBuilder 改写。

## 6. 遗留事项

- Batch 019 整体仍为 `in_progress`：前端记忆抽屉/UI 与跨层产品验收尚未在本阶段处理。
- 本阶段不做向量检索；Batch 027 负责评测和 feature flag。
- 分支采纳/版本选择沿用现有 workflow 约束，后续如开放并发分支需补 active-branch 专项测试。
