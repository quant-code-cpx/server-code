# alert测试方案-20260524

> 范围：`AlertController`（事件日历、异动监控、涨跌停、价格预警）
> 设计原则：全新业务视角；契约优先；不依赖既有 spec 结论。
> 状态：✅ 已回归

## 核心风险与本轮覆盖

- 权限边界：管理员接口（如 `/alert/anomalies/scan`）不可被普通用户调用。
- DTO 契约边界：`/alert/anomalies/detail` 必须要求 `anomalyId`。
- 分页上限防护：`/alert/limit-list` 的 `pageSize` 应受上限限制。

## 本轮执行用例（fresh）

- `ALT-V2-002` `/alert/calendar/list` 日期格式错误返回 `400` ✅
- `ALT-V2-003` 普通用户调用 `/alert/anomalies/scan` 返回 `403` ✅
- `ALT-V2-004` `/alert/anomalies/detail` 仅传 `id` 不应通过 ✅（本轮修复）
- `ALT-V2-005` `/alert/limit-list` `pageSize=201` 被限制到 `<=200` ✅

## 缺陷记录

| Bug ID | 严重度 | 优先级 | 标题 | 期望 | 实际 | 状态 | 修复 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ALT-BUG-001 | 高 | P0 | `/alert/anomalies/detail` 未对 `anomalyId` 做 DTO 校验 | 缺失必填字段时返回 400 | 接口返回 201 并继续执行 | FIXED | 新增 `MarketAnomalyDetailQueryDto` 并替换 Controller 入参 |

## 回归结果

- `pnpm exec jest test/fresh/alert-fresh-v2.spec.ts --runInBand` ✅
- `pnpm exec jest test/fresh --runInBand` ✅
- `pnpm build` ✅

## 深度压测批次补充（2026-05-24）

- 执行命令：`pnpm exec jest test/fresh/auth-alert-pressure-v1.spec.ts --runInBand`
- 结果：`ALT-LOAD-001 / ALT-STRESS-001` 全部通过。
- 说明：当前结果为控制器层基线压测（Mock 依赖，强调可重复回归与稳定性），可用于后续真实环境压测对比。
