# 量化研究后端 — 统一 TODO List

> **来源**：整合自 `planning/TODO.md`（功能规划）、`planning/SERVER_TODO.md`（工程质量）、`planning/FEATURE_GAP_ANALYSIS_2026-04-01.md`（产品痛点），原文件已归档至 `archive/`。
>
> **颗粒度约定**：每一项 TODO 应可独立出一份技术设计方案，且适合在**一次 agent 任务**中完成。
>
> 格式：`- [ ]` 待完成，`- [x]` 已完成。

---

## P0 — 核心链路瓶颈（不做则关键功能空转）

### 数据层补全

- [x] **同步 `stk_limit`（涨跌停价格）**：新增 Prisma Model、Tushare `stk_limit` 接口封装、注册到 SyncService 每日同步任务（设计文档：`数据层缺口设计.md` Phase 1）
- [x] **同步 `suspend_d`（停牌日历）**：新增 Prisma Model、Tushare `suspend_d` 接口封装、注册到 SyncService 每日同步任务（设计文档：`数据层缺口设计.md` Phase 1）
- [x] **同步 `index_weight`（指数成分权重）**：新增 Prisma Model、Tushare `index_weight` 接口封装、注册到 SyncService 定期同步任务（设计文档：`数据层缺口设计.md` Phase 1）
- [x] **数据校验层：Tushare 入库前清洗**：在 sync 流程中添加通用校验中间件，处理空值、异常值、重复记录，记录校验失败日志（设计文档：`数据层缺口设计.md` Phase 3）

### 回测撮合引擎

- [x] **日频撮合引擎核心实现**：实现按日模拟买卖，处理涨跌停不可交易、停牌跳过、滑点模型、手续费扣减（依赖 `stk_limit` / `suspend_d` 数据）（设计文档：`回测系统-后端设计.md`）
- [x] **组合管理与仓位跟踪**：实现多标的仓位记录、现金管理、权重再平衡逻辑，供撮合引擎调用（设计文档：`回测系统-后端设计.md`）
- [x] **策略信号生成框架**：将 4 种策略模板（均线交叉/选股轮动/因子排名/自定义池）翻译为可执行买卖信号（设计文档：`回测系统-后端设计.md` 第六节）
- [x] **基准对比与超额收益计算**：在回测流程中同步计算基准净值曲线，输出超额收益、跟踪误差（设计文档：`回测系统-后端设计.md`）
- [x] **风险指标计算模块**：实现 Sharpe / Sortino / Calmar / Max Drawdown / 年化波动率等指标的引擎内计算（设计文档：`回测系统-后端设计.md`）
- [x] **回测参数验证**：添加回测区间数据完整性检查、起止日期合理性校验、资金量边界校验（设计文档：`回测系统-后端设计.md` 第 8.2 节）

### 因子预计算

- [x] **因子值批量预计算定时任务**：每日收盘后批量计算全市场因子截面值并落库，避免查询时实时计算，为因子分析接口提供数据基础（设计文档：`因子研究闭环设计.md` Phase 1）

---

## P1 — 显著提升产品可用性与工程质量

### 数据接入

- [x] **同步龙虎榜数据（`top_list` / `top_inst`）**：新增 Prisma Model、Tushare 接口封装、注册同步任务（设计文档：`数据层缺口设计.md` Phase 2）
- [x] **同步大宗交易数据（`block_trade`）**：新增 Prisma Model、Tushare 接口封装、注册同步任务（设计文档：`数据层缺口设计.md` Phase 2）
- [x] **同步融资融券数据（`margin_detail`）**：新增 Prisma Model、Tushare 接口封装、注册同步任务（设计文档：`数据层缺口设计.md` Phase 2）
- [x] **同步沪深港通资金数据（北向/南向）**：新增 Prisma Model、Tushare 接口封装、注册同步任务、对外 API
- [x] **同步限售股解禁数据（`share_float`）**：新增 Prisma Model、Tushare 接口封装、注册同步任务（设计文档：`数据层缺口设计.md` Phase 2）
- [x] **同步概念板块映射（`ths_index` / `ths_member`）**：同花顺概念板块目录 + 成分映射，6000 积分。新增 `ThsIndex` / `ThsMember` Prisma Model、mapper、注册同步任务，提供概念列表 / 成分 / 个股所属概念查询接口（设计文档：`概念板块映射-后端设计.md`）

#### Tushare 数据扩展 — 第一批（核心因子 + 事件驱动）

- [x] **同步业绩预告（`forecast`）**：Tushare `forecast` 接口，2000 积分。新增 Prisma Model、mapper、注册到 FinancialSyncService 同步计划。财报季关键事件信号
- [x] **同步股东人数（`stk_holdernumber`）**：Tushare `stk_holdernumber` 接口，2000 积分。筹码集中度核心指标，选股因子
- [x] **同步沪深股通持股明细（`hk_hold`）**：Tushare `hk_hold` 接口，2000 积分。北向资金个股持仓变化，补全已有 `moneyflow_hsgt` 总量数据的个股维度
- [x] **同步指数每日指标（`index_dailybasic`）**：Tushare `index_dailybasic` 接口，4000 积分。指数级 PE/PB/换手率/股息率，市场估值分位和择时判断

#### Tushare 数据扩展 — 第二批（风控与分析体系完善）

- [x] **同步股东增减持（`stk_holdertrade`）**：Tushare `stk_holdertrade` 接口，2000 积分。重要股东行为信号
- [x] **同步股权质押统计（`pledge_stat`）**：Tushare `pledge_stat` 接口，2000 积分。质押比例风险信号
- [x] **同步财务审计意见（`fina_audit`）**：Tushare `fina_audit` 接口，2000 积分。非标审计风险信号
- [x] **同步财报披露计划（`disclosure_date`）**：Tushare `disclosure_date` 接口，2000 积分。预判财报窗口，事件驱动策略

#### Tushare 数据扩展 — 第三批（扩展策略维度）

- [x] **同步主营业务构成（`fina_mainbz`）**：Tushare `fina_mainbz` 接口，2000 积分。业务拆分分析
- [x] **同步申万行业分类（`index_classify` + `index_member_all`）**：2000 积分。标准行业分类体系
- [x] **同步股票回购（`repurchase`）**：Tushare `repurchase` 接口，2000 积分。回购事件因子
- [x] **同步可转债数据（`cb_basic` + `cb_daily`）**：2000 积分。可转债量化策略

### 指数模块

- [x] **创建 IndexModule 骨架与指数列表接口**：新建 `src/apps/index/` 模块，提供支持的指数列表查询接口（沪深300 / 中证500 / 上证50 / 创业板指等）
- [x] **指数日线行情接口**：封装 Tushare `index_daily`，同步指数日线数据，提供查询 API
- [x] **指数成分股查询接口**：基于已同步的 `index_weight` 提供指数成分股及权重查询 API

### 股票模块补全

- [x] **技术指标接口（MACD / RSI / KDJ / 布林带）**：本地纯函数计算实现（`stock/utils/technical-indicators.ts`），提供 `POST /stock/detail/analysis/technical` 查询 API（设计文档：`股票模块补全-后端设计.md`）

### 自选股与研究工作台

- [x] **自选股管理 CRUD**：新增 `Watchlist` + `WatchlistItem` Prisma Model，实现创建/查询/更新/删除自选股分组及标的（设计文档：`自选股与研究工作台设计.md` Phase 1）
- [x] **研究笔记 CRUD**：新增 `ResearchNote` Prisma Model，实现按股票/标签查询的笔记管理接口（设计文档：`自选股与研究工作台设计.md` Phase 2）
- [x] **选股条件订阅**：新增 `ScreenerSubscription` Model，支持保存筛选条件并设置定时执行推送（设计文档：`自选股与研究工作台设计.md` Phase 3）

### 安全加固

- [x] **JWT Secret 启动校验**：移除代码级默认值，缺失环境变量时启动报错
- [x] **细粒度限流**：添加每接口/每用户级别的限流策略（当前仅全局 20 req/10s）
- [x] **Redis ACL 密码环境变量化**：将 Redis 密码从硬编码 entrypoint 改为环境变量注入
- [x] **CORS 生产环境配置审查**：修复 `false` 作为 falsy 值的问题，配置明确的白名单
- [x] **请求体大小限制**：添加全局请求体大小限制，防止大 JSON 攻击
- [x] **管理员操作审计日志**：记录用户创建/删除/角色变更/密码重置等操作到审计表

### 可观测性（P0 级）

- [x] **健康检查端点**：使用 `@nestjs/terminus` 实现 `/health` + `/ready`，检查 DB / Redis / BullMQ 状态（已实现于 `src/shared/health/`）
- [x] **结构化日志**：Winston 输出 JSON 格式，包含 traceId / userId / latency 字段，便于日志聚合解析（已实现于 `src/shared/logger/logger.service.ts`、`src/shared/context/request-context.service.ts`）
- [x] **生产环境请求/响应日志**：扩展 LoggingInterceptor 支持生产环境，添加 `LOG_HTTP_REQUESTS` / `LOG_HTTP_BODY` 可配置开关（已实现于 `src/config/app.config.ts`）

### 测试覆盖

- [x] **Auth 模块单元测试**：覆盖登录、注册、Token 刷新、验证码、账户锁定流程
- [x] **User 模块单元测试**：覆盖 CRUD、角色权限校验、密码重置
- [x] **Stock 模块单元测试**：覆盖列表查询、搜索、详情接口
- [x] **Backtest 模块单元测试**：覆盖任务提交、引擎计算、指标计算（设计文档：`回测引擎详细设计.md` 第八节）
- [x] **Tushare Client 单元测试**：Mock HTTP 调用、频控重试逻辑
- [ ] **E2E 测试套件**：至少覆盖 Auth 流程、股票查询、回测提交等关键路径
- [x] **Jest 覆盖率阈值配置**：设置 >60% 阈值，集成到 CI

### CI/CD

- [x] **添加 CI 工作流**：创建 `.github/workflows/ci.yml`，包含 lint → build → test 流水线（PostgreSQL + Redis Service Container）
- [ ] **Docker 镜像构建工作流**：添加 GitHub Actions 工作流，构建并推送生产镜像到 GHCR
- [x] **Prisma migration 一致性检查**：CI 中验证 schema 与 migration 文件一致

### 生产部署

- [x] **生产环境 Dockerfile**：多阶段构建 `dockerfiles/app/Dockerfile.prod`（deps → builder → prod，非 root 用户运行）
- [x] **优雅关闭（Graceful Shutdown）**：`enableShutdownHooks()` + `RedisShutdownService` + `PrismaService.onModuleDestroy()`，SIGTERM/SIGINT 时依次释放 Redis → DB 连接

---

## P2 — 差异化竞争力

### 回测系统进阶

- [x] **回测结果持久化**：将回测结果写入 `BacktestResult` 数据库表，支持历史查询（设计文档：`回测系统-后端设计.md` 第九节）
- [x] **回测报告接口**：提供收益曲线、最大回撒、夏普比率、胜率等报告数据 API（设计文档：`回测系统-后端设计.md` 第八节）
- [x] **WebSocket 实时推送回测进度**：通过 Socket.IO 向前端推送回测执行进度百分比（设计文档：`回测系统-后端设计.md` 第十节）
- [x] **回测任务超时机制**：添加 BullMQ 任务超时配置，防止长时间挂起（设计文档：`回测引擎详细设计.md` 第 9.3 节）
- [x] **Walk-forward 验证**：实现样本内优化 + 样本外验证流程，防止过拟合（设计文档：`回测引擎详细设计.md` 第四节）
- [x] **多策略对比**：同一时间段多组回测结果并排比较接口（设计文档：`回测引擎详细设计.md` 第五节）

### 因子研究

- [x] **自定义因子表达式引擎**：支持用户通过表达式语言（如 `rank(close/delay(close,20))`）定义新因子，后端实时计算（设计文档：`因子研究闭环设计.md` Phase 2）
- [x] **因子正交化**：实现 Fama-MacBeth / 截面回归的多因子正交能力（设计文档：`因子研究闭环设计.md` Phase 4）
- [x] **因子到回测全链路**：打通“因子 → 选股 → 组合 → 回测”闭环（设计文档：`因子研究闭环设计.md` Phase 3）

### 行业轮动分析

- [x] **行业轮动分析模块**：行业指数收益对比（近 5/20/60 日）、行业动量排名、行业间资金流转分析、行业 PE/PB 历史分位（设计文档：`行业轮动分析-后端设计.md`）

### 组合管理与风控

- [x] **虚拟组合管理**：创建组合 → 添加/调整持仓 → 自动计算每日盈亏（已实现于 `src/apps/portfolio/portfolio.service.ts`，含加权平均成本合并、当日 P&L、历史净值曲线；设计文档：`组合管理与风控-后端设计.md`）
- [x] **组合风险分析接口**：行业分布、个股集中度、市值分布、Beta 暴露分析（已实现于 `src/apps/portfolio/portfolio-risk.service.ts`，含 HHI 集中度指数、沪深300 基准 Beta）
- [x] **风控规则引擎**：单票仓位上限、回撤止损线、行业集中度限制的规则管理（已实现于 `src/apps/portfolio/risk-check.service.ts`，含持仓变动自动检测、违规日志记录）

### 实时行情与预警

- [x] **价格预警规则管理**：新增 `PriceAlert` Model，支持设置涨跌幅阈值触发 WebSocket + 邮件通知
- [x] **异动监控引擎**：基于 moneyflow 数据，实现成交量异常放大、连板监测、大单成交预警
- [x] **事件日历接口**：聚合财报发布日、解禁日、分红除权日，提供日历视图 API

### 数据同步增强

- [x] **Tushare 并行批量同步**：TushareClient 改为按 API 名称独立节流通道（不同 API 可同时进行），sync.service.ts `runPlans()` 改为 Category 间并行执行（可配置 `TUSHARE_SYNC_CONCURRENCY`，默认 3）
- [x] **同步断点续传**：`SyncHelperService` 新增 `getResumeKey/updateProgress/markCompleted/resetProgress`；`syncByTradeDate` 每 50 个成功日期写一次断点，下次从断点日期+1恢复
- [x] **同步进度百分比估算**：`EventsGateway` 新增 `broadcastSyncProgress`/`broadcastSyncOverallProgress`；`runPlans()` 注入节流 `onProgress` 回调（2s 或 5% 阈值）；`syncByTradeDate` 每个分片后回调
- [x] **失败任务自动重试队列**：新增 `TushareSyncRetryQueue` Prisma Model（含指数退避）；`SyncRetryService` 每 5 分钟扫描 PENDING 记录消费；管理端 `POST /tushare/admin/retry-queue` 和 `/retry-queue/reset` 接口
- [x] **同步日志查询接口**：`SyncLogService` 实现分页查询和任务状态汇总；管理端 `POST /tushare/admin/sync-logs` 和 `/sync-logs/summary` 接口

### 架构优化

- [x] **拆分 `stock.service.ts`**：拆分为 `StockListService` / `StockDetailService` / `StockMoneyFlowService` / `StockFinancialService` / `StockScreenerService`，原 `StockService` 改为 Facade（131 行），零行为变更
- [x] **WebSocket 推送用户/角色过滤**：同步状态消息应仅推送给管理员，非广播给所有连接（设计文档：`自选股与研究工作台设计.md` 第 7.2 节）

### 可观测性（P1 级）

- [x] **Prometheus 指标接入**：使用 `@willsoto/nestjs-prometheus` 暴露 HTTP 延迟、QPS、错误率、队列深度（已实现于 `src/shared/metrics/metrics.module.ts`，含 HTTP / BullMQ / Prisma / Tushare 指标）
- [x] **Grafana 仪表盘**：Docker Compose 添加 Grafana + Prometheus，预置量化后端仪表盘模板（已实现：`docker-compose.yml` monitoring profile + `monitoring/` 目录含 Prometheus 配置、Grafana 数据源/仪表盘自动配置）
- [x] **数据库查询性能日志**：添加慢查询告警（Prisma query event 监听）（已实现于 `src/shared/prisma.service.ts`，可配置 `PRISMA_SLOW_QUERY_THRESHOLD_MS`，默认 500ms）
- [x] **Tushare 同步耗时与成功率监控**：记录每次同步任务的耗时、成功/失败状态并暴露指标（已实现于 `src/shared/metrics/metrics.constants.ts`，含 TUSHARE_SYNC_DURATION / TUSHARE_SYNC_TOTAL 等 Prometheus 指标）

### 数据管理

- [x] **手动触发单表同步接口**：管理端 `POST /tushare/admin/sync` 已支持 `tasks: TushareSyncTaskName[]` 参数，可指定单表或多表同步
- [x] **数据完整性监控**：自动发现某只股票某天数据缺失，生成完整性检查报告（设计文档：`数据层缺口设计.md` Phase 3）
- [ ] **历史行情分区表**：对 `daily` / `weekly` / `monthly` 等大表按年份做分区，优化查询性能

---

## P3 — 锦上添花

### 高级数据接入

- [x] **同步 ETF/LOF 净值数据（`fund_nav` / `fund_daily`）**：新增 Prisma Model、Tushare 接口封装、同步任务（已实现于 `fund-api.service.ts` / `fund-sync.service.ts`，含 `fund_basic` 基础资料同步）
- [x] **同步宏观经济指标（`cn_cpi` / `cn_ppi` / `cn_gdp` / `shibor`）**：新增 Prisma Model、Tushare 接口封装、同步任务（已实现于 `macro-api.service.ts` / `macro-sync.service.ts`）
- [x] **同步期权数据（`opt_basic` / `opt_daily`）**：新增 Prisma Model、Tushare 接口封装、同步任务（已实现于 `option-api.service.ts` / `option-sync.service.ts`）

### 策略管理

- [x] **创建 StrategyModule CRUD**：新增 `Strategy` Prisma Model（name / description / params / userId），实现策略创建/读取/更新/删除
- [x] **策略参数 JSON Schema 校验**：回测策略配置 JSON 添加 Schema 校验，拒绝不合法的参数配置
- [x] **策略草稿自动保存**：新增 `StrategyDraft` Model，支持未完成的回测参数配置暂存与加载（设计文档：`自选股与研究工作台设计.md` Phase 4）

### 热力图增强

- [x] **HeatmapSnapshot 聚合模型**：新增 Prisma `HeatmapSnapshot` + `HeatmapSnapshotStatus` Model，已实现于 `prisma/heatmap_snapshot.prisma`
- [x] **热力图多维度分组**：支持按行业 / 概念板块 / 指数三种分组维度（设计文档：`热力图增强-后端设计.md` Phase 1）
- [x] **历史热力图快照接口**：查询指定日期的热力图聚合数据（设计文档：`热力图增强-后端设计.md` Phase 3）

### 事件驱动研究

- [x] **事件影响分析（Event Study）**：实现事件前后 N 日超额收益统计，支持业绩预告/分红公告/股东增减持等事件类型（已实现于 `src/apps/event-study/`，含 `POST /event-study/analyze` + `POST /event-study/events` + `GET /event-study/event-types`；设计文档：`事件驱动研究-后端设计.md`）
- [x] **事件信号生成器**：符合条件的事件自动触发选股信号（已实现于 `src/apps/event-study/event-signal.service.ts`，含规则 CRUD、条件匹配、每日 19:00 定时扫描、WebSocket 推送）

### 高级量化工具

- [x] **蒙特卡洛模拟**：随机重采样估算策略鲁棒性（设计文档：`回测引擎详细设计.md` 第六节）
- [x] **相似 K 线形态匹配**：基于 DTW 或归一化欧氏距离的全市场历史形态搜索
- [ ] **智能选股助手（LLM 驱动）**：接入 LLM，支持自然语言选股并转换为选股器 JSON 配置
- [x] **量化报告生成引擎**：提供研究报告模板机制，自动拉取数据组装 PDF / Markdown / HTML 报告

### 数据同步管理台

- [x] **数据同步状态总览接口**：各数据表最后同步时间、数据行数、缺失天数的聚合查询
- [x] **异常数据标记与修正入口**：管理端可标记异常数据并提供人工修正接口（设计文档：`数据层缺口设计.md` Phase 3）

### 文档与开发体验

- [x] **补充 .env.example 注释**：为各环境变量添加用途说明
- [x] **API 变更日志（CHANGELOG）**：建立接口变更记录规范（根目录 `CHANGELOG.md`，Keep a Changelog 格式，记录全量接口目录与变更规范）
- [x] **Swagger 文档补充响应示例**：为核心接口补充更多响应示例（portfolio 控制器 17 个接口添加 `@ApiSuccessResponse` + `@ApiBearerAuth`，创建 `portfolio-response.dto.ts`；auth / backtest / stock / market 响应 DTO 补充 `example` 字段）
- [x] **TypeScript any 类型清理**：逐步消除 ~182 处 `any` 类型，添加明确的接口定义

### 风控模块

- [x] **创建 RiskModule 骨架**：风控能力已集成于 `src/apps/portfolio/` 模块（`PortfolioRiskService` + `RiskCheckService`），不再单独建 `src/apps/risk/`
- [x] **组合持仓风险检测接口**：实现集中度 / 行业暴露 / Beta 检测（已实现于 `portfolio-risk.service.ts`，4 个分析维度）
- [x] **止损触发规则管理**：支持配置和管理止损触发规则（已实现于 `risk-check.service.ts`，含 MAX_DRAWDOWN_STOP / MAX_SINGLE_POSITION / MAX_INDUSTRY_WEIGHT 三类规则）

### 通知系统

- [ ] **站内消息系统**：基于 WebSocket 的站内消息推送机制
- [ ] **邮件 / 微信 Webhook 告警**：可选的外部通知渠道集成

---

## 已完成模块参考（不再跟踪）

<details>
<summary>点击展开已完成项</summary>

- [x] NestJS 骨架（ConfigModule / ThrottlerModule / Helmet / CORS）
- [x] Prisma + PostgreSQL 接入
- [x] Redis 接入
- [x] JWT 鉴权体系（AccessToken + RefreshToken）
- [x] 全局日志（Winston + 日志轮转）
- [x] 全局异常过滤器 & 响应拦截器
- [x] BullMQ 回测任务队列
- [x] WebSocket 实时推送
- [x] TushareService — 封装 Tushare Pro HTTP 接口基础调用
- [x] TushareSyncService — 应用启动时检测数据新鲜度 + 交易日 18:30 定时同步
- [x] 已接入 22 类 Tushare 数据（stock_basic / trade_cal / daily / weekly / monthly / adj_factor / daily_basic / moneyflow 等）
- [x] 用户管理模块全部完成（CRUD / 角色权限 / 软删除 / @CurrentUser）
- [x] 股票列表 / 详情 / 搜索 / K线 / 资金流 / 财务 / 股东分红接口
- [x] 市场资金流向（大盘 / 行业板块）
- [x] 热力图数据接口骨架
- [x] 选股器（选股策略保存 / 执行）
- [x] 因子库（12 分类，IC 分析 / 分位回测 / 衰减分析 / 分布 / 相关矩阵）
- [x] 回测框架骨架（BullMQ 任务提交 / 状态查询）
- [x] 缓存策略（Redis 缓存 + 同步后失效 + 命中率监控）
- [x] 错误处理统一（BusinessException / Tushare 错误码 / 参数边界校验）
- [x] 数据库索引优化 + 连接池配置
- [x] ESLint `no-explicit-any` warn 级别已启用

</details>

---

_最后更新：2026-04-11（Grafana 仪表盘完成，P1 可观测性 4 项全部完成）_
