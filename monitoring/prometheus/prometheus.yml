global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/agent-alerts.yml

scrape_configs:
  - job_name: 'quant-server'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['app:3000']
        labels:
          instance: 'quant-app'
