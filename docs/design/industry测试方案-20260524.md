# Industry 模块 API 测试方案-20260524

> 范围：Industry（行业字典）
> 设计原则：全新开始；业务场景优先；黑盒推导期望结果；不以现有实现或现有测试为正确性依据。
> 状态：已完成

---

## 1. 测试范围

| 项目 | 内容 |
| ---- | ---- |
| 模块 | Industry - 行业字典 |
| 接口列表 | 1 个端点 |
| 用户角色 | 公共端点（无需认证） |
| 依赖数据 | sw_industry_classification, sector_capital_flows, stock_basic_profiles, sw_industry_members |

### 接口清单

| 分类 | 端点 | 说明 |
| ---- | ---- | ---- |
| 字典映射 | dict-mapping | 申万 L1 → 东财行业板块映射 |

---

## 2. 核心业务规则

| 规则编号 | 规则 | 来源 | 测试影响 |
| -------- | ---- | ---- | -------- |
| R-IN01 | source 可选，必须是 'sw_l1' | DTO @IsIn(['sw_l1']) | 非法值应 400 |
| R-IN02 | target 可选，必须是 'dc_industry' | DTO @IsIn(['dc_industry']) | 非法值应 400 |
| R-IN03 | includeUnmatched 可选，@Type(Boolean)+@IsBoolean | DTO @Type(Boolean) @IsBoolean | @Type(Boolean) 将任意值转为 boolean，@IsBoolean() 始终通过，无法触发 400 |
| R-IN04 | 空请求体走默认参数（source=sw_l1, target=dc_industry, includeUnmatched=true） | DTO default 值 | 空体应 201 |
| R-IN05 | 返回 items 数组，每项含 matchType 和 confidence | 响应 DTO | 结构校验 |
| R-IN06 | coverage 包含 total/matched/unmatched/matchRate | 响应 DTO | 结构校验 |
| R-IN07 | 无认证要求，公共端点 | 无 @UseGuards | 无需 Token |

---

## 3. 测试用例矩阵

### 字典映射

| 用例ID | 类型 | 优先级 | 场景 | 期望结果 | 状态 |
| ------ | ---- | ------ | ---- | -------- | ---- |
| IN-BIZ-001 | BIZ | P0 | 空请求体查询字典映射 | 201, items+coverage, 默认参数 | 通过 |
| IN-BIZ-002 | BIZ | P0 | 显式传入合法参数 | 201, 透传给 service | 通过 |
| IN-BIZ-003 | BIZ | P0 | includeUnmatched=false | 201, 过滤未匹配项 | 通过 |
| IN-BIZ-004 | BIZ | P0 | 返回数据结构完整性 | 201, 含 source/target/version/tradeDate/coverage/items | 通过 |
| IN-ERR-001 | ERR | P1 | 非法 source | 400 | 通过 |
| IN-ERR-002 | ERR | P1 | 非法 target | 400 | 通过 |
| IN-ERR-003 | ERR | P1 | source 和 target 同时非法 | 400 | 通过 |
| IN-ERR-004 | ERR | P1 | includeUnmatched 任意值经 @Type(Boolean) 均被接受（DTO 设计特征） | 201, 值被转为 boolean | 通过 |
| IN-EDGE-001 | EDGE | P1 | service 抛异常 | 500 | 通过 |
| IN-EDGE-002 | EDGE | P1 | service 返回空数据 | 201, 空 items, 零 coverage | 通过 |
| IN-SEC-001 | SEC | P0 | 无 Token 访问 | 201（公共端点，无需认证） | 通过 |

---

## 4. 测试执行报告

> 执行时间：2026-05-24
> 执行方式：Jest controller spec + Test.createTestingModule + mock services + supertest

### 执行总览

| 模块 | 用例数 | 通过 | 失败 | 跳过 | 通过率 |
| ---- | ------ | ---- | ---- | ---- | ------ |
| Industry | 11 | 11 | 0 | 0 | 100% |

### 备注

- IN-ERR-003/004 调整：`includeUnmatched` 字段使用 `@Type(() => Boolean)` + `@IsBoolean()`，class-transformer 会将任意值转为 boolean（truthy → true, falsy → false），导致 `@IsBoolean()` 始终通过。已用同时非法 source+target 和 DTO 行为文档化两条用例替代原计划。
