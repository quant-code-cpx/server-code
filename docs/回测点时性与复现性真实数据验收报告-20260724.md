# 回测点时性与复现性真实数据验收报告（2026-07-24）

## 1. 结论

Batch 029 已完成本地真实 PostgreSQL 迁移、旧数据保全、财务修订全量刷新、三类代表性真实 Run、缺失点时股票池的 fail-closed 补强，以及目标页面三态真实浏览器验收。批次本地验收完成；未提交、未推送、未触发 GitHub 或生产发布。

## 2. 数据库迁移与保全

- 迁移：`20260723000000_add_backtest_reproducibility_metadata` 已在本地真实测试库执行成功。
- 迁移前备份：`../data/postgres/codex-backups/batch029-20260723-pre-migration/backtest-and-financial.dump`。
- 备份权限：`0600`；SHA-256 与 `pg_restore --list` 均通过。
- 迁移后：
  - 7/7 个历史 Run 保留，全部标为 `LEGACY_UNVERIFIED`；
  - 256,924/256,924 条财务指标保留；
  - 财务修订唯一索引 1 个、PIT 查询索引 3 个均存在。

## 3. 真实 Run 差异

### 3.1 HS300 选股轮动

旧 Run：`cmpbacquc000xpkiregiytna2`，`LEGACY_UNVERIFIED`。

新 Run：`cmrxp7bef0000a5ne99mub42l`，`VERIFIED`。

- 新 Run 输入 SHA-256：`84d01d2c7193e2a0f020e55764da664e2fe4be0629f56295c515114975e8ae1a`
- 有效重叠区间：2025-07-01 至 2026-05-18，共 212 个净值日。
- 旧口径区间收益：7.8465%；新 PIT 口径：7.2712%；差异 -0.5753 个百分点。
- 旧口径成交 54 笔；新口径成交 66 笔。
- 新 Run 保存 39 个有序 universe snapshot，`qualityFlags=[]`。

本地 `000300.SH` 成分快照仅覆盖 2025-06-30 以后。因此 2023-05-18 起跑的同配置复刻曾解析出空股票池。验收据此新增两道门禁：

- readiness 必须找到“指定指数、回测起始日当日或更早”的快照；
- engine 任一日期得到空股票池时抛出 `4005 BACKTEST_UNIVERSE_DATA_UNAVAILABLE`，禁止生成伪 `VERIFIED` 结果。

发现缺口时生成的 Run `cmrxp3mr50000a5bn18b11lp8` 已保留为反例，并标记：

- `reproducibilityStatus=LEGACY_UNVERIFIED`
- `qualityFlags=["PIT_INDEX_COVERAGE_GAP"]`

### 3.2 单股 MA / QFQ

旧 Run：`cmps4aaj6000xp54f4wukuxkb`，`LEGACY_UNVERIFIED`，收益 0、成交 0。

新 Run：`cmrxp8hsy0000a53f2osjp06g`，`VERIFIED`。

- 输入 SHA-256：`679c0c6d5cc6e83da53611b4b8bd78c39089c2dfd12935662d4998274a1ae20e`
- 区间：2020-01-01 至 2024-12-31。
- 股票：`601398.SH`；为避免本地 2020 年 HS300 成分快照缺失，验收配置显式使用单股 `CUSTOM` universe。
- 新口径收益 3.3760%，最大回撤 -1.8692%，成交 34 笔。
- 保存 120 个 universe snapshot，`qualityFlags=[]`。

该组用于验证 QFQ 修复后的真实运行链和 manifest 持久化；因 universe 从历史 Run 的 HS300 改为显式单股 CUSTOM，不把收益差异解释为纯复权公式贡献。

### 3.3 ROE 公告点时与修订版本

真实刷新首次执行时发现 `mapFinaIndicatorRecord` 会读取 `update_flag`，但 `TUSHARE_FINA_INDICATOR_FIELDS` 未请求该字段；刷新后的 `update_flag` 因此仍全部为 null。该轮结果未被接受，已停止并补上请求字段及回归测试后从头重刷。

修复后 `000001.SZ` 已真实返回并保存同一报告期、同一公告日的 `update_flag=0/1` 两个版本。点时查询实测：

- 2025-04-18 仍返回 2024 年报，ROE 9.2038；
- 2025-04-19 公告日切换到 2025 一季报，ROE 2.8165；
- 同公告日存在多个版本时确定性优先 `update_flag=1`。

代表 Run：`cmrxq1fv10000a5ahkji5absj`，`VERIFIED`。

- 策略：ROE 因子排名；显式 CUSTOM universe `000001.SZ`。
- 区间：2024-01-01 至 2026-05-18。
- 输入 SHA-256：`a805bd79e75554b1ff0c9f42bf8c22c01e95d0bd3a402a042461ccf80164e81f`
- 收益 36.1438%，最大回撤 -19.0308%，58 个 universe snapshot，`qualityFlags=[]`。
- 单股票因子 Run 仅用于验证公告点时、修订版本和持久化全链，不作为策略收益优劣结论。

全量刷新最终结果：

- 候选股票：5,805 只；近两年 7 个报告期；
- 写入：34,395 条；`failedCount=0`；
- 财务指标总量：263,263 条；
- `update_flag=1` 27,752 条，`update_flag=0` 6,643 条；
- 6,182 个同证券/报告期存在多个修订版本；
- 同步日志状态：`SUCCESS`，`failed=[]`。

## 4. 自动验证

已通过：

- Batch 029 golden/readiness：27/27；
- engine 单测：42/42；
- 财务同步/mapper：55/55；
- 最终关键联合回归：6 suites、130/130；
- 前端复现性警示组件：4/4；
- Batch 027 可选 embedding fetch 注入修复：6/6；
- 后端生产构建；
- Batch 029 修改的业务源文件 ESLint；
- AppModule 最小启动与本地 API 完整启动；
- Compose `app/worker/agent-worker/scheduler` 已恢复；app、PostgreSQL、Redis 健康，三个工作进程无启动错误；
- 旧 Run 保持 unverified、新 Run 完整 manifest、覆盖缺口反例保持 fail-closed。

自动验证还包括真实 readiness 正反分支：2023-05-18 的 HS300 起始日前无快照，被正确拒绝；2025-07-01 有覆盖，验证通过。

真实 Chrome 页面验收通过，三个页面均无浏览器 warning/error：

1. `cmrxp7bef0000a5ne99mub42l` 显示“结果已通过可复现性校验”，并展示 engine/data/universe/financial/QFQ 五项版本；
2. `cmpbacquc000xpkiregiytna2` 显示“旧回测或可信元数据不完整”、五项版本缺失及 `LEGACY_UNVERIFIED`；
3. `cmrxp3mr50000a5bn18b11lp8` 显示风险警告、完整版本及 `PIT_INDEX_COVERAGE_GAP`，未被误标为 verified。

`git diff --check` 通过。

## 5. 发布状态

- 本报告只证明本地真实数据、服务和页面验收，不代表 GitHub 或生产发布验收。
- 本次未提交、未推送、未触发真实发布或部署。
