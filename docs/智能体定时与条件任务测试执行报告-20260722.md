# 智能体定时与条件任务测试执行报告-20260722

> 范围：Batch 020 基础实现。执行时间：2026-07-22。
> 结论：核心代码、独立 PostgreSQL/Redis 集成、契约、lint 与构建均通过。Docker Desktop VM 仍不可用，但不阻断本 Batch 的隔离验证。

## 已验证

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| Prisma schema | 通过 | `pnpm prisma:generate`、`pnpm prisma validate` |
| 条件 DSL | 通过 | 仅 `DAILY_CLOSE` + `GT/GTE/LT/LTE`；SQL/未知字段和字符串分钟数被拒绝 |
| Cron/时区 | 通过 | 上海 18:30 逻辑触发点换算单元测试 |
| 竞争去重 | 通过 | 双 scanner 单元 + 独立 PostgreSQL CAS；同一逻辑时点仅一条 execution |
| 冷却/水位 | 通过 | 冷却不建 execution；缺 DAILY watermark 记录 `DEFERRED`，不入 Agent |
| Run/outbox 接口 | 通过 | `sendScheduled` 原子创建会话、Run、Event 与 outbox；executionId 为幂等边界，预算冻结 |
| BullMQ | 通过 | 临时 Redis 验证重复 job、retry identity、authority 恢复；真实 PostgreSQL outbox 可入队 |
| 租户隔离 | 通过 | 独立 PostgreSQL 验证 task/execution 不向其他 userId 暴露 |
| API 契约 | 通过 | `pnpm swagger:generate`、`pnpm agent:contracts:generate/check` |
| Lint 基线 | 通过 | `pnpm lint`，无新增 lint 分组 |
| TypeScript | 通过 | `pnpm run build` |

执行命令：

```text
pnpm prisma:generate
pnpm prisma validate
pnpm exec jest src/apps/scheduled-research/test src/apps/agent/application/test/agent-run-scheduled.spec.ts --runInBand
RUN_SCHEDULED_RESEARCH_DB_INTEGRATION=true DATABASE_URL=<临时本机 PostgreSQL> pnpm exec jest src/apps/scheduled-research/test/scheduled-research.repository.integration.spec.ts --runInBand
RUN_AGENT_DB_INTEGRATION=true RUN_AGENT_QUEUE_INTEGRATION=true AGENT_QUEUE_REDIS_URL=<临时本机 Redis> DATABASE_URL=<临时本机 PostgreSQL> pnpm exec jest src/apps/agent/application/test/agent-interaction.repository.spec.ts src/queue/agent/test/agent-queue.integration.spec.ts --runInBand
pnpm swagger:generate
pnpm agent:contracts:generate && pnpm agent:contracts:check
pnpm lint
pnpm run build
```

本模块单元：4 suites、8 tests 通过；独立 PostgreSQL：3 tests 通过；Agent Run/outbox/BullMQ：13 tests 通过。

## 已修复缺陷

| ID | 严重度 | 修复 |
| --- | --- | --- |
| SCH-001 | P1 | REST 契约补充 `POST /api/agent/schedules/run`，使用 `clientRequestId` 幂等。 |
| SCH-002 | P1 | `cooldownMinutes`、`maxAgeMinutes` 先做 number narrowing，阻止字符串绕过。 |
| SCH-003 | P1 | deferred condition data 缺失时恢复后重新判定条件，不把未验证触发直接入队。 |

## 未执行与阻断

`quant_postgres` 仍 unhealthy：容器内 `pg_isready -U postgres -d quant_db` 返回 `no response`；PostgreSQL data bind mount 读取阻塞。重启 database 容器无法结束卡住进程；Docker Desktop 又被 macOS `virtualization.framework` 阻止启动 VM。未执行会删除数据的 Docker Reset。

为避免本机 Docker 故障阻断验证，启动了隔离的 Homebrew PostgreSQL 16（临时目录、`127.0.0.1:55432`）与 Redis（临时目录、`127.0.0.1:56379`）。全量 39 migrations、`prisma migrate status`、定时任务 CAS/unique/owner 集成和 Agent outbox→BullMQ 恢复均通过。HTTP 全应用 E2E、fake provider E2E、PERF/LOAD/STRESS 未执行，未伪造结果。
