# calendar测试方案-20260524

> 范围：`CalendarController`（交易日区间、近期日历）
> 设计原则：全新业务视角；契约优先；黑盒验证。
> 状态：✅ 已回归

## 核心风险与本轮覆盖

- 未登录访问受限接口必须拒绝。
- 日期格式错误必须被 DTO 校验拦截。
- 默认参数与边界值（如 `days`）需稳定。

## 本轮执行用例（fresh）

- `CAL-V2-001` 无 token 访问 `/calendar/range` 返回 `403` ✅
- `CAL-V2-003` `/calendar/range` 日期格式错误返回 `400` ✅
- `CAL-V2-004` `/calendar/upcoming` 默认 `days=30` ✅
- `CAL-V2-005` `/calendar/upcoming` `days=366` 返回 `400` ✅

## 缺陷记录

- 本轮未发现新增缺陷。

## 回归结果

- `pnpm exec jest test/fresh/calendar-fresh-v2.spec.ts --runInBand` ✅
- `pnpm exec jest test/fresh --runInBand` ✅
- `pnpm build` ✅
